/**
 * Funciones
 * Declarativa
 */

let persona = "Javier";

function saludo(nombre){ // funcion es una tarea especifica
     
   return `Hola ${nombre}`;
}

var saludar = saludo(persona); //invocar la funcion

console.log(saludar);